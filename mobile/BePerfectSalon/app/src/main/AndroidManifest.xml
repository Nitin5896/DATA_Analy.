<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- No CALL_PHONE permission: "Call the Salon" uses ACTION_DIAL, which opens the
         dialer pre-filled and doesn't need it. Keeping the permission list minimal avoids
         an unnecessary sensitive-permission declaration in Play Console review. -->

    <application
        android:name=".BePerfectSalonApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.BePerfectSalon">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BePerfectSalon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
